package bto.Enums;

/**
 * Represents the available types of flats in the system.
 * <p>
 * This enum defines the different flat configurations that can be
 * selected or assigned within the application.
 */
public enum FlatType {
    /** Represents a flat with two rooms. */
    TWO_ROOM,

    /** Represents a flat with three rooms. */
    THREE_ROOM
}